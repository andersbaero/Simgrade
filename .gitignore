node_modules/
web/dist/
*.log

# Executable build outputs
build/
dist/

# Downloaded per-install by `npm run setup` (52 MB of binaries + a 3 MB database),
# pinned by data/release.json so any clone reproduces the same versions.
bin/
data/db.json

# Your characters and your run state — personal, and meaningless to anyone else.
data/profiles/
data/profiles.json
data/settings.json
data/cache/
# The single-character layout this replaced, in case an old checkout still has it.
data/profile.json
data/selection.json
data/bench.json
data/config.json
data/last-run.json

# Kept in the repo on purpose:
#   data/profile.example.json  demo profile the tests load as a fixture
#   data/release.json          the pinned wowsims release
